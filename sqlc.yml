version: "2"

sql:
  - engine:  "postgresql"
    queries: "database/queries"
    schema:  "database/migrations"

    gen:
      go:
        package: "sqlc"
        sql_package: "pgx/v5"
        out: "database/sqlc"
